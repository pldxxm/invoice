<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/header') %>
  <body>
    <main class="flex min-h-screen flex-col p-4">
      <div
        class="flex h-24 shrink-0 items-end rounded-lg bg-red-500 p-4 text-5xl"
      >
        <div class="flex flex-row items-center text-white">
          <i class="fa-solid fa-exclamation-triangle pr-4"></i>
          Error
        </div>
      </div>

      <div class="mt-8 flex flex-col items-center justify-center">
        <div class="text-center max-w-2xl mx-auto">
          <div class="mb-8">
            <i
              class="fa-solid fa-circle-exclamation text-8xl text-red-500 mb-4"
            ></i>
            <h1 class="text-4xl font-bold text-gray-800 mb-4">
              <%= typeof errorTitle !== 'undefined' ? errorTitle : 'Something
              went wrong' %>
            </h1>
            <p class="text-xl text-gray-600 mb-6">
              <%= typeof errorMessage !== 'undefined' ? errorMessage : 'An
              unexpected error occurred. Please try again.' %>
            </p>
          </div>

          <div class="space-y-4">
            <% if (typeof errorCode !== 'undefined' && errorCode ===
            'invalid_pagination') { %>
            <div
              class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6"
            >
              <div class="flex">
                <i
                  class="fa-solid fa-info-circle text-yellow-600 mt-1 mr-3"
                ></i>
                <div class="text-left">
                  <h3 class="text-lg font-medium text-yellow-800 mb-2">
                    Invalid Pagination Parameters
                  </h3>
                  <p class="text-yellow-700">
                    The page number or limit you provided is not valid. Please
                    use positive whole numbers only.
                  </p>
                  <ul
                    class="list-disc list-inside mt-2 text-sm text-yellow-700"
                  >
                    <li>Page numbers must be 1 or greater</li>
                    <li>Limit must be between 1 and 100</li>
                    <li>Decimal numbers are not allowed</li>
                  </ul>
                </div>
              </div>
            </div>
            <% } %>

            <div class="flex flex-col sm:flex-row gap-4 justify-center">
              <a href="/" class="btn btn-primary">
                <i class="fa-solid fa-home mr-2"></i>
                Go to Home
              </a>
              <a href="javascript:history.back()" class="btn btn-outline">
                <i class="fa-solid fa-arrow-left mr-2"></i>
                Go Back
              </a>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include('../partials/toastScript') %>
  </body>
</html>
